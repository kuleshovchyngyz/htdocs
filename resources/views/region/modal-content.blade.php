<div class="modal-content__hidden modal-content--destroy-region">
	<div class="modal-title">{{ __('Delete Region') }}</div>
	<div class="modal-content">
	    {{ __('Do you really want to delete this region?') }}
	</div>

	<div class="modal-footer">
	    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
	    <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
	</div>
</div>

<div class="modal-content__hidden modal-content--archive-region">
	<div class="modal-title">{{ __('Archive Region') }}</div>
	<div class="modal-content">
	    {{ __('Do you really want to archive this region?') }}
	</div>

	<div class="modal-footer">
	    <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
	    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
	</div>
</div>