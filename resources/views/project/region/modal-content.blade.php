<div class="modal-content__hidden modal-content--destroy-project-region">
	<div class="modal-title">
		{{__('Delete Project Region')}}
	</div>

	<div class="modal-content">
		{{__('Do you really want to do this?')}}
	</div>

	<div class="modal-footer">
	    <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
	    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
	</div>
</div>


<div class="modal-content__hidden modal-content--destroy-competitor">
    <div class="modal-title">
        {{__('Delete Competitor')}}
    </div>

    <div class="modal-content">
        {{__('Do you really want to do this?')}}
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
    </div>
</div>

<div class="modal-content__hidden modal-content--destroy-schedule">
    <div class="modal-title">
        Удалить план
    </div>

    <div class="modal-content">
        {{__('Do you really want to do this?')}}
    </div>

    <div class="modal-footer">
        <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
        <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
    </div>
</div>

<div class="modal-content__hidden modal-content--destroy-project-region">
	<div class="modal-title">{{ __('Delete Project Region') }}</div>
	<div class="modal-content">
	    {{ __('Do you really want to do this?') }}
	</div>

	<div class="modal-footer">
	    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
	    <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
	</div>
</div>

<div class="modal-content__hidden modal-content--archive-project-competitor">
	<div class="modal-title">{{ __('Archive/Unarchive') }}</div>
	<div class="modal-content">
	    {{ __('Do you really want to do this?') }}
	</div>

	<div class="modal-footer">
	    <button type="button" class="btn btn-primary">{{ __('OK') }}</button>
	    <button type="button" class="btn btn-secondary" data-dismiss="modal">{{ __('Cancel') }}</button>
	</div>
</div>
